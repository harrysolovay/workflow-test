{
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/plugins/*",
    "packages/utils"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.4.0"
  },
  "scripts": {
    "release": "yarn changeset publish --follow-tags"
  }
}
